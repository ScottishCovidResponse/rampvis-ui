import { CardHeader } from "@material-ui/core";
function GraphTitle() {
  return (
    <CardHeader
      id="chartTitle"
      sx={{
        textAlign: "center",
      }}
    />
  );
}

export default GraphTitle;
