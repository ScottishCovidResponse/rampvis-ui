import { CardHeader } from "@mui/material";
function GraphTitle() {
  return (
    <CardHeader
      id="chartTitle"
      sx={{
        textAlign: "center",
      }}
    />
  );
}

export default GraphTitle;
